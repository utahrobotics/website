{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.4.1",
    "react": "^16.12.0",
    "react-bootstrap": "^1.0.0-beta.16",
    "react-dom": "^16.12.0",
    "react-scripts": "^3.3.0",
    "react-setup": "^3.9.0"
  },
  "scripts": {
    "start": "set HTTPS=true && set PORT=80 && START \"\" react-scripts start & node ./move_server.js",
    "start-linux": " HTTPS=true PORT=80 react-scripts start &; node ./move_server.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
